# FlowAI Environment Variables
# Copy to .env and fill in values — NEVER commit .env

# Optional: override DuckDB path
# DUCKDB_PATH=/path/to/custom/flowai.duckdb

# Optional: override Synthea output path
# SYNTHEA_OUTPUT=/path/to/synthea/output/csv
